<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CardSlider">
        <!-- Active card offset from start of RecyclerView. Default value is 50dp. -->
        <attr name="activeCardLeftOffset" format="dimension"/>
        <!-- Card width. Default value is 148dp. -->
        <attr name="cardWidth" format="dimension"/>
        <!-- Distance between cards. Default value is 12dp. -->
        <attr name="cardsGap" format="dimension"/>
        <!-- ViewUpdater implementation class. If not set, DefaultViewUpdater class is used. -->
        <attr name="viewUpdater" format="string"/>
    </declare-styleable>


    <!-- Attrs Time -->
    <declare-styleable name="TimeCircleView">
        <attr name="timeCircleColor" format="reference|color" />
        <attr name="timeCenterColor" format="reference|color" />
    </declare-styleable>

    <declare-styleable name="TimeRadialSelectorView">
        <attr name="timeSelectorColor" format="reference|color" />
    </declare-styleable>

    <declare-styleable name="TimeRadialNumbersView">
        <attr name="timeCircularNumbersColor" format="reference|color" />
    </declare-styleable>

    <declare-styleable name="TimeAmPmCirclesView">
        <attr name="timeAmPmBackgroundColor" format="reference|color" />
        <attr name="timeAmPmSelectBackgroundColor" format="reference|color" />
        <attr name="timeAmPmHighlightSelected" format="boolean" />
        <attr name="timeAmPmTextColor" format="reference|color" />
    </declare-styleable>

    <!-- Attrs Years -->
    <declare-styleable name="ListPickerYearView">
        <attr name="minYear" format="reference|integer" />
        <attr name="maxYear" format="reference|integer" />
        <attr name="defaultYear" format="reference|integer" />
    </declare-styleable>

    <!-- Attrs Year Text Indicator -->
    <declare-styleable name="YearTextIndicator">
        <attr name="yearSelectBackgroundColor" format="reference|color" />
        <attr name="yearSelectTextColor" format="reference|color" />
    </declare-styleable>

    <!-- Attrs Year background circle -->
    <declare-styleable name="TextCircularIndicatorView">
        <attr name="colorCircleIndicator" format="reference|color" />
    </declare-styleable>

</resources>